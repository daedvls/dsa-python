# used to hide data from the users in difft ways
# (JUST THEORY)

"""
Encapsulation means we hide the data and the functions that operate on that data
from the user. This is done to prevent the user from accessing the data directly and to provide a
controlled way of accessing the data.

(Example is 1_access_specifiers.py)
Where __password can't directly be accessed from outside the class. Instead, we provide a public method
getter and setter to access and modify the password.


Abstraction means we hide the implementation from the user and only provide the functionality to the user.
This is just a fancy way of saying that there exist "functions" in python lol.

(where we can *use* the function even if we didn't know the code within the function definition.)

"""